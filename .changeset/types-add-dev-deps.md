---
"pacquet": minor
---

Added `pnpm add --types`, which saves the matching `@types/*` package to `devDependencies` when the requested package does not ship its own `types` or `typings` declarations.
